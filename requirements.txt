# Распознавание речи оффлайн
vosk>=0.3.45

# Микрофон. Раньше в списке стоял pyaudio, но код его никогда не
# использовал — он работает через sounddevice. К тому же pyaudio 0.2.11
# на Windows требует сборки из исходников и падает при установке.
sounddevice>=0.4.6

# Интерфейс
customtkinter>=5.2

# Горячие клавиши и буфер обмена
pynput>=1.7
pyperclip>=1.8

# Вставка текста в активное окно
pyautogui>=0.9.54

# Для разработки:
#   pip install pytest
